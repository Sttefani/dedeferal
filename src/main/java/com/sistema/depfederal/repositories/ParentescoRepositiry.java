package com.sistema.depfederal.repositories;

import com.sistema.depfederal.models.Parentesco;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ParentescoRepositiry extends JpaRepository<Parentesco, Long> {
}
